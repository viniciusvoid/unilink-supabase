# Frontend estático — servido via Python (sem build) na Railway
[phases.setup]
nixPkgs = ["python3"]

[start]
cmd = "python3 -m http.server $PORT --directory ."

[variables]
NIXPACKS_PYTHON_VERSION = "3.11"
